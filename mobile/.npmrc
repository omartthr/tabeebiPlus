# Prevent npm from hoisting packages to parent workspace root.
# This ensures react-native and all deps are in mobile/node_modules/
# which is critical for EAS Android builds (Gradle expects them here).
workspaces=false
install-strategy=nested
legacy-peer-deps=true
